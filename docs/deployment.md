# Deployment

Status: stub, created in Phase 0. See [`../docker-compose.yml`](../docker-compose.yml) and
[`../.env.example`](../.env.example) for the local stack. Production deployment notes land in
Phase 8.
